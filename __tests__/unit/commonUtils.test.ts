import { expect, test } from 'vitest'
import { humanizePrice } from "@/src/utils/commonUtils"
 
test('Common Utility Functions - Humanizes Price', () => {
    const r = 1+1;
    expect(r).toBe(2);
    // const testEnPrice = 2500000;
    // const correctResult = "۲,۵۰۰,۰۰۰";

    // const result = humanizePrice(testEnPrice);
    // expect(result).toBe(correctResult);
})